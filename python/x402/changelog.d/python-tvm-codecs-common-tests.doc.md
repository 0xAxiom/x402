Add 41 unit tests for `mechanisms/tvm/codecs/common.py` — the only file in `mechanisms/tvm/codecs/` without a dedicated test module. Covers `normalize_address` (raw passthrough, `Address` object input, user-friendly to raw conversion, parity between string and object inputs, invalid-address rejection), `address_to_stack_item` (slice typing, base64 BoC round-trip via `Cell.one_from_boc`, user-friendly input acceptance), `decode_base64_boc` (round-trip with `encode_base64_boc`, non-string rejection covering bytes/None/int, invalid-base64 rejection), `make_zero_bit_cell` (Cell instance, single zero-bit payload, determinism, parity with manual `Builder().store_bit(0).end_cell()`), `encode_base64_boc` (string return type, exact byte parity, round-trip with richer cells), `get_network_global_id` (mainnet `-239`, testnet `-3`, positive ID, missing/uppercase prefix rejection, non-numeric rejection), `parse_amount` (decimal-to-smallest-unit, zero, zero-decimals, sub-unit truncation, nine-decimal native TON amounts, non-numeric rejection), and `parse_money_to_decimal` (int/float passthrough, `$` prefix, USD/USDT and lowercase suffixes with and without separating whitespace, combined prefix+suffix, surrounding whitespace, float type guarantee, unparseable rejection).
