Add 29 unit tests for `mechanisms/svm/exact/register.py` covering `register_exact_svm_client` (default `solana:*` wildcard + V1 fanout, str/list networks, policy registration, `rpc_url` propagation to V2 and V1 client schemes, sync variant), `register_exact_svm_server` (V2-only registration, str/list networks, sync variant), and `register_exact_svm_facilitator` (str/list networks, V1 fanout across all `V1_NETWORKS`, V2 and V1 schemes sharing a single `SettlementCache`, independent caches per call, sync variant) — the only file in `mechanisms/svm/exact/` without a dedicated unit test module.
